name: Infrastructure Tests
on: push

jobs:
  opentofu_test:
    name: "Run OpenTofu tests"
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v3
        with:
          # TODO: fill in your IAM role ARN!
          role-to-assume: arn:aws:iam::968196829199:role/lambda-sample-tests
          role-session-name: tests-${{ github.run_number }}-${{ github.actor }}
          aws-region: us-east-2

      - uses: opentofu/setup-opentofu@v1

      - name: Tofu Test
        working-directory: 2_tds/td5/scripts/tofu/live/lambda-sample
        env:
          TF_VAR_name: lambda-sample-${{ github.run_id }}
        run: |
          tofu init -backend=false -input=false
          tofu test -verbose
